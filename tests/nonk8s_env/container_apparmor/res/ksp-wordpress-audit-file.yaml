apiVersion: security.kubearmor.com/v1
kind: KubeArmorPolicy
metadata:
  name: ksp-wordpress-audit-file
spec:
  severity: 6
  selector:
    matchLabels:
      kubearmor.io/container.name: wordpress-mysql
  file:
    matchPaths:
    - path: /etc/passwd
  action:
    Audit
